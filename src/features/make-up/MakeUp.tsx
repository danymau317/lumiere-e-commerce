import NavBar from "../../components/Layout/NavBar/NavBar";

type MakeUpProps = {};

export default function MakeUp({}: MakeUpProps) {
  return (
    <section>
      <header>
        <NavBar />
      </header>
      <p>Make up</p>
    </section>
  );
}
